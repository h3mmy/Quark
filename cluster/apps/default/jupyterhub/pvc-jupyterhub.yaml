apiVersion: v1
kind: PersistentVolumeClaim
metadata:
 name: pvc-jupyterhub
 namespace: default
spec:
 storageClassName: oci-fss
 accessModes:
   - ReadWriteMany
 resources:
  requests:
    storage: 2Gi
 volumeName: jupyterhub-shared
